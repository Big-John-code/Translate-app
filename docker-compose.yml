services:
  viewer:
    build: ./docker
    ports:
      - "3000:80"
    volumes:
      - ./books:/data/books:ro
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
